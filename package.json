{
  "name": "multi-agent-leads-crm",
  "private": true,
  "scripts": {
    "mcp:install": "cd mcp-server && npm install",
    "mcp:start": "cd mcp-server && npm start",
    "mcp:health": "powershell -NoProfile -Command \"Invoke-RestMethod http://localhost:3001/health\""
  }
}
